namespace QueryMiddleware
{
    /// <summary>
    /// Query string names which we define here.  These are magic strngs and so we want to define them
    /// once only, so we can easily change it if we want
    /// </summary>
    public static class QueryStringNames
    {
        public const string Message = "qm";
    }
}
